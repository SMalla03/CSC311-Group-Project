# Random Forest Performance Summary

The random forest was trained using the `full` feature set with `50` trees. On the latest run, it reached `0.9796` training accuracy, `0.9018` validation accuracy, and `0.9006` test accuracy.

On the test set, the strongest class by F1 was `The Persistence of Memory` with precision `0.9322`, recall `0.9649`, and F1 `0.9483`. The weakest class was `The Starry Night` with precision `0.9200`, recall `0.8070`, and F1 `0.8598`.

Use the full training report for confusion matrices and the feature summary to see which inputs the forest relied on most.