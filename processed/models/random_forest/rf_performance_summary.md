# Random Forest Performance Summary

The random forest was trained using the `full` feature set with `50` trees. On the latest run, it reached `0.8830` training accuracy, `0.8690` validation accuracy, and `0.8713` test accuracy.

On the test set, the strongest class by F1 was `The Persistence of Memory` with precision `0.8710`, recall `0.9474`, and F1 `0.9076`. The weakest class was `The Starry Night` with precision `0.9091`, recall `0.7018`, and F1 `0.7921`.

Use the full training report for confusion matrices and the feature summary to see which inputs the forest relied on most.