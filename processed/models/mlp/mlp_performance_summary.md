# MLP Performance Summary

The sklearn MLP was trained on the full saved preprocessing pipeline and reached `0.9296` training accuracy, `0.8810` validation accuracy, and `0.9006` test accuracy on the latest run.

On the test set, the strongest class by F1 was `The Persistence of Memory` with precision `0.9167`, recall `0.9649`, and F1 `0.9402`. The weakest class was `The Starry Night` with precision `0.8750`, recall `0.8596`, and F1 `0.8673`.

Use the full training report for confusion matrices and the weight summary for the strongest hidden-unit patterns.